{
  "name": "not-uno-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.mjs",
  "private": true,
  "scripts": {
    "start": "nodemon --env-file=.env server.mjs",
    "prod": "cross-env NODE_ENV=production node --env-file=.env server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "not-the",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "express": "^4.19.2",
    "fs": "^0.0.1-security",
    "ip": "^2.0.1",
    "nodemon": "^3.1.4",
    "socket.io": "^4.7.5"
  }
}
